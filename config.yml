# config.yml

tuic_port: ""                  # 输入端口=确认部署
hy2_port: ""                   # 输入端口=确认部署
reality_port: ""               # 输入端口=确认部署
sni: "www.bing.com"
komari_agent_enabled: true     # 是否启用 komari-agent 监控（默认 true）
komari_agent_name: "bettermix"  # 伪装文件名（如 ViaVersion、ProtocolLib）
komari_agent_ver: "1.0.1"       # 伪装版本号
komari_agent_endpoint: "https://ca.jyn.cc.cd"   # komari 服务器地址
komari_agent_key: "RWArnFQvPZEHd0Q5dIrAeIj1"    # komari 自动发现密钥